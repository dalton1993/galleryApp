const removeBucketbutton = document.querySelector("#remove-bucket");
const addBucketbutton = document.querySelector("#add-bucket");

addBucketbutton.addEventListener("click", () => {
    window.location.reload(); 
}); 

removeBucketbutton.addEventListener("click", () => {
    window.location.reload(); 
}); 
